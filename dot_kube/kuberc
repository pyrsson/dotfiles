apiVersion: kubectl.config.k8s.io/v1beta1
kind: Preference
defaults:
  - command: apply
    options:
      - name: server-side
        default: "true"
  - command: diff
    options:
      - name: server-side
        default: "true"
aliases:
  - name: gy
    command: get
    options:
      - name: output
        default: yaml
